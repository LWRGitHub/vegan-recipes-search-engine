import React from 'react';
import Header from './Header';

const Home = () => (
    <div className="m- p-0">
        <Header page={'Home'}/>
        <section className="container">
        </section>
    </div>
);

export default Home;